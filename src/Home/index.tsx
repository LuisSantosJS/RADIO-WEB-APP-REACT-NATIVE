import React, { useEffect } from 'react';
import { View, Button } from 'react-native';
import Stream from '../PlayerStream';

const Home: React.FC = () => {
    useEffect(() => {
        return () => {
            
        }
    }, []);

    return (
        <View>
            <Stream/>
        </View>
    );
}
export default Home;















